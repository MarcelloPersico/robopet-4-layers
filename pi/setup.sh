#!/usr/bin/env bash
# Idempotent first-time install for the Pi side. Run as the pet user with sudo
# privileges. Installs apt deps, creates a venv at /opt/pet/venv, enables
# systemd units. Concrete steps to be filled in during M3.
set -euo pipefail
echo "M3: implement Pi setup script."
exit 1
